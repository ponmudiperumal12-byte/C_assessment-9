#include <stdio.h>

int main()
{
    int a[5], i, biggest;

    printf("Enter the numbers:\n");

    for(i = 0; i < 5; i++)
        scanf("%d", &a[i]);

    biggest = a[5];

    for(i = 1; i < 5; i++)
    {
        if(a[i] >  biggest)
            biggest = a[i];
    }

    printf("%d", biggest);
}
